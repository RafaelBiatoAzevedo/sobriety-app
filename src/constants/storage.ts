const PREFIX = "@sobriety";

export const STORAGE_KEYS = {
  USER: `${PREFIX}:user`,
  CURRENT_SOBRIETY_START: `${PREFIX}:currentSobrietyStart`,
  HISTORY: `${PREFIX}:history`,
  SETTINGS: `${PREFIX}:settings`,
};
